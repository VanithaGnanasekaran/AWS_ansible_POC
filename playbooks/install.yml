---
- name: Install Tomcat
  hosts: role_tomcat
  become: true
  gather_facts: false

  tasks:
    - name: Install Java
      ansible.builtin.apt:
        name: openjdk-17-jdk
        state: present
        update_cache: true

    - name: Install Tomcat
      ansible.builtin.apt:
        name: tomcat10
        state: present
        update_cache: true

    - name: Start Tomcat
      ansible.builtin.service:
        name: tomcat10
        state: started
        enabled: true


- name: Install Apache
  hosts: role_apache
  become: true

  tasks:
    - name: Install Apache
      ansible.builtin.apt:
        name: apache2
        state: present
        update_cache: true

    - name: Start Apache
      ansible.builtin.service:
        name: apache2
        state: started
        enabled: true
